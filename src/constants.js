import { floorPlanerScale } from './utils/blueprint/core/constants';

export const decimalPlaces = 2;
export const backendUrl = 'http://127.0.0.1:4000/products';
export const groundLayerHeight = 0;
export const floorLayerHeight = 6 * floorPlanerScale;
export const itemLayerHeight = 12 * floorPlanerScale;
